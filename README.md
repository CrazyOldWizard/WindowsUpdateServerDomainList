# WindowsUpdateServerDomainList
A list of domains that Windows uses for Windows Update


This is a list of domains that can be added as a blacklist for pihole.  This should block windows from updating.
